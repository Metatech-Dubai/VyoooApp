/// Font family names registered in [pubspec.yaml].
abstract final class AppFonts {
  /// Headlines, marketing titles (Figma: Inter Semi Bold).
  static const String display = 'Inter';

  /// Body, inputs, buttons, labels (Figma: DM Sans).
  static const String body = 'DM Sans';
}
